cd

unzip ~/Dropbox/id_rsa.zip

git clone git@github.com:unkei/dotfiles.git

ln -s dotfiles/.bash_profile
ln -s dotfiles/.vimrc
ln -s dotfiles/.vim

git submodule init (for NeoBundle.vim in .vim/bundle/neobundle.vim)
vim (to perform NeoBundleInstall)

